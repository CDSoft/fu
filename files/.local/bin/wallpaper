#!/bin/bash

color="srgb(16,32,96)"

convert -size 640x360 \
    plasma:$color-$color \
    -virtual-pixel tile \
    -blur 0x4 \
    /tmp/wallpaper.png

DISPLAY=%(os.getenv "DISPLAY") feh --no-fehbg --bg-fill /tmp/wallpaper.png
