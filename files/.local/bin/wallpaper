#!/bin/bash

convert -size 640x360 \
    plasma:ForestGreen-ForestGreen \
    -virtual-pixel tile \
    -blur 0x4 \
    /tmp/wallpaper.png

DISPLAY=%(os.getenv "DISPLAY") feh --no-fehbg --bg-fill /tmp/wallpaper.png
